# 本地音频剪切工具 - 项目规范

## 1. 项目概述

- **项目名称**: 音频剪切器
- **项目类型**: 单页Web应用（纯前端）
- **核心功能**: 读取本地音频文件，播放并设置起止时间截取音频片段，导出为WAV或MP3格式
- **目标用户**: 需要对音频进行简单剪切处理的用户

## 2. 技术方案

- **前端框架**: 原生 HTML + CSS + JavaScript（无外部依赖）
- **音频处理**: Web Audio API
- **音频导出**:
  - WAV格式: 使用原生JavaScript实现
  - MP3格式: 使用 lamejs 库（CDN引入）
- **波形显示**: Canvas绘制

## 3. 功能列表

### 3.1 音频加载
- 通过文件选择器读取本地音频文件
- 支持多种主流音频格式：MP3、WAV、OGG、AAC、M4A、FLAC、WMA、APE
- 加载后显示音频波形图

### 3.2 音频播放
- 加载后自动启用原音频播放功能
- 播放控制：播放/暂停按钮
- 显示当前播放时间
- 播放进度与波形同步

### 3.3 时间选择
- 显示音频总时长
- 输入框设置起始时间和结束时间
- 可视化波形上标记截取区间
- 支持拖拽选择区间
- **新增**: 点击"设置起始时间"按钮将当前播放位置设为起始时间
- **新增**: 点击"设置结束时间"按钮将当前播放位置设为结束时间
- 播放过程中可实时查看和调整截取区间

### 3.4 音频截取
- 点击截取按钮执行剪切操作
- 实时预览截取后的音频
- 显示截取片段的时长

### 3.5 音频导出
- 支持导出为WAV格式
- 支持导出为MP3格式
- 点击按钮触发文件保存

## 4. UI设计

### 4.1 布局结构
- 简洁的单页布局
- 垂直排列：标题 → 文件选择 → 播放控制 → 波形显示 → 时间控制 → 操作按钮 → 预览/导出

### 4.2 配色方案
- 主色调: #4A90E2（蓝色）
- 背景色: #F5F5F5（浅灰）
- 卡片背景: #FFFFFF（白色）
- 文字色: #333333（深灰）
- 强调色: #67CB73（绿色，用于截取按钮）
- 危险色: #E74C3C（红色，用于删除/重置）

### 4.3 组件设计
- 文件选择器: 原生input[type=file]样式优化
- 波形区域: 灰色背景，蓝色波形，选中区域高亮，播放进度指示
- 播放控制: 播放/暂停按钮，当前进度显示
- 时间输入: 数字输入框，搭配标签
- 快捷按钮: "设为起始"和"设为结束"按钮
- 按钮: 圆角按钮，统一样式
- 导出选项: 下拉选择框或按钮组

## 5. 验收标准

1. ✅ 能够选择并加载本地音频文件
2. ✅ 支持多种主流音频格式（MP3、WAV、OGG、AAC、M4A、FLAC、WMA、APE）
3. ✅ 加载后可以播放原音频
4. ✅ 播放时显示当前播放进度
5. ✅ 能够设置和调整起始/结束时间
6. ✅ 支持通过播放位置快速设置起始/结束时间
7. ✅ 点击截取按钮能正确提取音频片段
8. ✅ 截取后可以预览播放
9. ✅ 能够将截取后的音频导出为WAV格式
10. ✅ 能够将截取后的音频导出为MP3格式
11. ✅ 页面布局简洁美观
12. ✅ 所有操作都有适当的用户反馈
